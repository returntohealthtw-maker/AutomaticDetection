<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sh.simpleeeg">

    <!-- 網路上傳（傳送腦波數據到後端） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!--
        ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION：
        Android 6 ~ 11 做 BLE 掃描必須要 FINE_LOCATION，
        否則在小米 / 紅米 / OPPO 等 OEM 上會 silently fail，無法取得電量。
        Android 12+ 已用 BLUETOOTH_SCAN(neverForLocation) 取代，因此限制 maxSdkVersion=30。
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="30"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- 自動更新 APK 需要：下載 + 詢問使用者安裝 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="false"/>
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false"/>

    <application
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">


        <activity
            android:name=".setting" android:exported="true" />
        <activity
            android:name=".test" android:exported="true" />
        <activity
            android:name=".pretest" android:exported="true" />
        <activity
            android:name=".threshold" android:exported="true" />
        <activity
            android:name=".multi_time" android:exported="true" />
        <activity android:name=".waiting"  android:exported="true" />
        <activity android:name=".flash"  android:exported="true" />
        <activity android:name=".report"  android:exported="true" />
        <!-- 前端原型 WebView App（目前的啟動頁，方便測試付款流程） -->
        <activity
            android:name=".WebAppActivity"
            android:exported="true"
            android:label="腦波檢測系統"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|uiMode|locale|fontScale|navigation|density">
            <!--
                configChanges：把所有 configuration change 都自己處理，
                避免 Activity 被系統重建 → WebView 重新載入 → 重複彈「發現新版本」
                + 主程式記憶體狀態（顧問登入、檢測中資料）被清空。
                平板從橫向轉直向 / 折疊裝置變形 / 系統字級 / 暗色模式 都不會重建 Activity。
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 原有腦波偵測 App（保留，未來整合用） -->
        <activity
            android:name=".main" android:exported="true">
            <!-- 已移除 LAUNCHER，不再顯示在桌面 -->
        </activity>

        <!-- 自動更新：把下載到 app cache 的 APK 用 FileProvider 分享給系統安裝程式 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.sh.simpleeeg.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>