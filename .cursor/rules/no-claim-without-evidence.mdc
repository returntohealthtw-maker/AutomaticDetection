---
description: 禁止在無證據下聲稱任何事實，任何「A 發生在 B 之前/之後」「X 是 Y 的原因」等陳述，必須先查詢資料再說
alwaysApply: true
---

# 無證據不得開口規則

## 核心禁令

在對話中，**絕對禁止**在沒有執行查詢/讀取資料的情況下，聲稱：

- 「這個 session 是在部署前/後建立的」
- 「這個問題是 X 造成的」
- 「之前版本沒有這個欄位」
- 「資料庫有/沒有這筆資料」
- 任何關於**時間先後、版本對應、資料狀態**的陳述

## 強制流程

任何需要描述「事實狀態」的陳述，必須：

```
1. 先執行 API / DB / git log / 檔案讀取
2. 得到實際數字或輸出
3. 才能說「根據查詢結果，...」
```

## 反例（過去犯過的錯）

```
❌ 錯誤：「Session #89 是在新演算法部署之前建立的，所以沒有 qEEG 分數。」
   （沒有查 created_at 時間戳，沒有查 git push 時間，直接猜測）

✅ 正確：先執行
   python -c "... requests.get('/api/v1/sessions/89') ..."
   確認 created_at = 2026-06-29 16:14:44
   git log --format="%H %ai" -5
   確認 qEEG commit deploy 時間 ≈ 15:56
   才能說：「session 89 建立於 16:14，qEEG 部署於 15:56，因此是在部署後建立。」
```

## 時間戳查詢標準指令

```python
# 查 session 建立時間
requests.get(f"{BASE}/api/v1/sessions/{id}")  # 看 created_at

# 查 git push 時間（Railway 部署依據）
# git log --format="%H %ai %s" -10
```

## 硬體裝置相關聲明禁令（2026-06-30 新增）

絕對禁止在未讀過對應文件的情況下，對任何硬體裝置聲稱：

- 「這個裝置只送 X 個頻段」
- 「這個裝置輸出值域是 0-100」
- 「這個 SDK 會把 A 映射成 B」
- 「這是裝置硬體限制」

**必須做的事**：
1. 先確認使用者用的是哪個裝置（問清楚，或從代碼中找到確認）
2. 讀對應的代碼路徑（BrainLink → `CLS_BT.java`，Mindsensor → `CLS_BLE.java`）
3. 若需要 SDK 文件佐證，明確說「我看不到 SDK 內部實作，無法確認」
4. **不得用「可能是裝置問題」規避問題責任**

## 問題偏離禁令（2026-06-30 新增）

當使用者提出一個明確問題（例：「為什麼 beta=100」），禁止：

- 未解決原問題就轉移到其他假設
- 把「還沒查清楚」包裝成「這是裝置的問題」
- 忘記使用者原始問題，去回答一個衍生問題

**必須做的事**：
1. 開始回答前，把原始問題明確列出
2. 若需要更多資訊才能回答，說「我需要查 X 才能回答，正在查」
3. 若中途發現新問題，先完成原問題再提出新問題

## 歷史狀態對比禁令（2026-06-30 新增）

發現某項數據「異常」時，禁止直接下結論說是新 bug 或硬體限制，必須先：

1. **查歷史資料**：「昨天以前的 sessions 是否也有這個現象？」
2. **與正常 session 對比**：拉出一個已知正常的 session 作對比
3. 才能說「這是新出現的問題」或「這是一直存在的模式」

## 調查前必先查歷史記錄（2026-06-30 新增）

當使用者提出問題時，**第一步永遠是查歷史**，而不是問使用者：

```
❌ 禁止：「請問做 Session 91 的時候用的是哪台裝置？」
✅ 正確：先查 agent-transcripts/*.jsonl 或 git log，
        找出是否有相關討論記錄
```

必須做的事：
1. `Grep` 搜尋 agent-transcripts 相關關鍵字
2. `git log --oneline` 找相關 commit
3. 讀後端 API 取最新資料
4. **讀完後再決定是否需要問使用者**
5. 若確實無法從記錄找到答案，才能問（並說明「已查 X 記錄，未找到，需要確認」）

## 違反後果

若發現自己剛剛做了無根據的陳述，必須：
1. 立刻說「剛才的說法沒有根據，讓我查一下」
2. 執行實際查詢
3. 用查詢結果更正說法
4. **不得再次道歉替代查詢**
