---
description: 計畫完成後必須等使用者確認才能開始執行
alwaysApply: true
---

# 計畫確認強制規則

## 核心規則：計畫完成 ≠ 可以執行

無論在 Plan mode 或 Agent mode，只要輸出的內容是「計畫」性質（例如：多步驟實作方案、架構調整方案、含有多個檔案改動清單的方案），完成計畫陳述後，**必須停下來等待使用者明確表示同意**（例如「可以」「開始」「執行」「好」），才能開始：

- 修改任何檔案
- 執行任何會改變系統狀態的指令（git commit、部署、DB 寫入、API 呼叫等）
- 建立/標記 TodoWrite 為 `in_progress` 並實際動手做

## 例外

- 若使用者在同一則訊息中，已經明確要求「請直接開始執行」「不用等我確認，做就對了」，才可以直接執行，不需要再次停下確認。
- 純讀取/搜尋/分析（Read、Grep、Glob、唯讀 Shell 指令）不受此規則限制，可以在計畫階段自由使用以蒐集資訊。

## 違反過的案例（2026-07-31）

在 `CreatePlan` 完成計畫後，使用者尚未回覆確認，AI 已切換為 Agent mode 並開始執行 todo 的第一步（讀取檔案並準備修改），造成使用者需要緊急喊停「等等，先別執行」。

## 正確流程

```
1. 完成計畫（Plan mode 內用 CreatePlan 呈現）
2. 停下，不呼叫任何修改性工具
3. 等待使用者輸入
4. 使用者明確同意（"可以" / "開始" / "執行" / "好的做吧"）後，才開始執行 todo
```
