---
description: 追蹤「討論承諾的功能」是否真正實做完成，防止只討論不實做
alwaysApply: true
---

# 實做追蹤與審查規則

## 核心原則：說到做到，做完要審查

任何對話中「我們會做 X」「我來實做 Y」「這個要加到資料庫」等承諾，
**必須在同一段對話內完成，或明確說明「本次未做，待下次」並列入 TODO**。

## 強制流程

### 實做前
- 承諾任何功能前，先確認目前程式碼狀態（讀相關檔案）
- 對話中出現「要存入資料庫」「要顯示在頁面上」「要計算 X」→ 立即建 TodoWrite

### 實做中
- 每個 TODO 項目必須實際寫出程式碼並確認存檔
- 不得「只說結構」而跳過實際 code；不得「假設已做好」

### 實做後（必做）
每次實做結束前，必須逐一審查：

```
已承諾的功能：
✅ [已實做] 說明在哪個檔案的哪一行
❌ [未實做] 明確說明「本次未做」並說明原因
⚠️ [部分實做] 說明做了什麼、遺漏了什麼
```

## 常見遺漏模式（過去犯過的錯）

| 情境 | 錯誤 | 正確 |
|-----|------|------|
| 資料庫欄位 | 說「加一個欄位」但沒寫 migration | 寫進 `_run_lightweight_migrations` 並確認 |
| 前後端同步 | 只改後端 Python，沒同步前端 JS | 明確說「前後端都已修改」並列出兩個檔案 |
| Firebase 同步 | 加了 PostgreSQL 欄位但沒同步到 Firebase | 同步更新 `firebase_sync.py` |
| 說「已修正」| 只改了一端或邏輯有誤 | 有驗證結果（print/測試輸出）才說已修正 |

## 路徑釐清責任

存在多條數據路徑時，必須明確說明每條路徑的狀態：

- Android App → `/sessions/upload` → 180 筆 EegCapture ✅ 已存個別記錄
- 前端網頁 → `/eeg/save-stats` → 1 筆平均值 + raw_arrays_json（設計如此）

**不得含糊帶過，讓使用者誤以為「兩條路徑都做了」。**

## 未完成的功能必須顯示在對話末

每次結束對話前，若有未完成的承諾，最後必須列出：

> **本次未完成（下次繼續）**：
> - [ ] 功能 A：原因是 X
> - [ ] 功能 B：原因是 Y
