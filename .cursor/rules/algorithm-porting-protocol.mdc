---
description: 複雜演算法移植工作協議（防止分批實作導致遺漏）
alwaysApply: true
---

# 複雜演算法移植工作協議

## 核心原則：先全部讀懂，再動筆實作

移植外部演算法（如 BrainDNA）時，**禁止「邊讀邊實作」**。
過去因此犯過的錯誤：
- 讀了 proportionRange 函式，但沒讀調用它的 calcXxx 方法，漏掉兩步驟
- 讀了 calcBand 選窗邏輯，但沒確認分母是否截斷，算出錯誤比例
- 讀了 maxArray 用途，但沒確認 MindColor 例外使用 mindArray（全部視窗）

## 實作前強制清單

在開始寫任何程式碼前，必須完成：

- [ ] 讀完所有相關函式的**完整實作**（不只函式簽名）
- [ ] 列出每個步驟的**輸入類型、是否截斷、輸出值域**
- [ ] 確認**前後端的資料格式**是否一致
- [ ] 標出哪些計算用「部分資料」、哪些用「全部資料」
- [ ] 把完整資料流展示給使用者確認後，才開始實作

## 修改規則

- 演算法相關函式前後端必須**同步修改**（不可只改 Python 或只改 JS）
- 不得使用 `×1.1 / ×0.9` 等估算邏輯替代實際演算
- 不得因「大部分正確」就省略邊界條件的確認

## 除錯規則

- debug 代理人用於「找出哪裡結果不對」，但**無法替代實作前的完整讀取**
- 結果看起來合理不代表算法正確（例如 alpha 值域對但比例算法錯）
- 發現數值異常時，優先對照原始碼逐行確認，不是猜測原因

## 溝通規則

- 複雜演算法實作前，主動向使用者展示「完整步驟清單」請求確認
- 不得對使用者說「已修正」，除非前後端都已同步修正且已驗證
- 若有多個相互依賴的步驟，一次說清楚所有步驟，不分批告知
