---
description: 程式修改與執行規範 — 任何動作執行前必須取得明確授權
alwaysApply: true
---

# 程式修改與執行規範

## 核心原則：執行前必須先問，等「可以」才動

❌ **絕對禁止**：
- 診斷/分析問題後，自行決定執行修復
- 「順便」做任何沒被明確要求的事
- 為了「驗證」效果而觸發任何生產環境的操作
- 未經授權執行任何腳本、API 呼叫、或批次操作

✅ **必須做**：
- 分析完問題後，說明「我打算做 X，原因是 Y，可以嗎？」
- 等使用者明確回答「可以」或「好」，才執行
- 若使用者只說「解釋原因」，只分析，不執行任何動作

## 需要事前授權的所有動作（不限於以下）

| 動作類型 | 範例 | 是否需要先問 |
|---|---|---|
| 修改程式碼 | 修改任何 .py .js .ts .html 檔案 | ✅ 必須先問 |
| git 操作 | commit、push、branch | ✅ 必須先問 |
| 執行腳本 | 跑任何 .py 腳本 | ✅ 必須先問 |
| 呼叫生產 API | POST/PATCH/DELETE 任何端點 | ✅ 必須先問 |
| 觸發外部服務 | 報告生成、Firebase 寫入、GCS 操作 | ✅ 必須先問 |
| 批次操作 | 一次影響多個 session/客戶的動作 | ✅ 必須先問（需特別說明影響範圍與費用） |
| 資料庫寫入 | 更新任何 DB 欄位 | ✅ 必須先問 |

## 批次操作的特別規定

批次操作（影響多個客戶資料或產生費用的操作）必須：
1. 說明「影響哪些 session / 幾個客戶」
2. 說明「大約產生多少費用（API token、運算時間）」
3. 等使用者明確授權後才執行
4. 絕不能在「驗證」過程中順帶觸發

## 今日違規紀錄（2026-07-21）

在未獲授權的情況下，觸發了 21 個舊客戶 session 的報告重新生成：
- 消耗了大量 Gemini API token（24 章節 × 21 份報告）
- 消耗了 Railway 數小時的運算資源
- 清除了 21 份原有的已完成報告 PDF 連結
- 這是嚴重違規，造成使用者的實際金錢損失

## 修改後：自動驗證清單

### 後端（Python/Railway）修改後
1. `git diff` 確認只改了被授權的部分
2. 語法檢查：`python -m py_compile <changed_file>`
3. 告知使用者「已修改，要驗證嗎？」，等確認再打 API
4. git push 後等待 Railway 部署，再次打端點確認（需使用者授權）

### 部署前確認事項

- [ ] 只修改了被授權的檔案
- [ ] 沒有動到不相關的邏輯
- [ ] 本地驗證通過
- [ ] 告知使用者「已驗證，準備部署，確認嗎？」

## 緊急修復流程

即使緊急，仍必須：
1. 先說明「問題是 X，打算改 Y，預期結果是 Z，影響範圍是 W」
2. 等使用者確認
3. 改完後執行自動驗證
4. 回報驗證結果後再 push
