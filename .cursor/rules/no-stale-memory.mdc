---
description: 禁止引用舊記憶，必須以實際程式碼為唯一事實來源
alwaysApply: true
---

# 禁止引用舊記憶，程式碼才是事實

## 核心規則：「不讀不說」

在陳述任何數值、邏輯、參數、欄位狀態之前，**必須先用 Read/Grep 工具讀取實際檔案確認**，絕不依賴：
- 對話摘要中的舊描述
- 上一段對話的記憶
- 「我印象中是…」「上次我們改成…」

## 強制執行場景

| 情境 | ❌ 禁止 | ✅ 必須 |
|---|---|---|
| 說明某個常數的值 | 「成人 delta CAP 是 98K」（從記憶） | 先 `Read braindna_algorithms.py` 確認 |
| 說明某功能是否已實作 | 「上次我們已經改好了」 | 先 `Grep` 確認程式碼中存在 |
| 比較兩個版本的差異 | 靠摘要描述 | 直接讀兩個版本的實際行數 |
| 回答「目前版本是多少」 | 靠記憶回答 | 先讀 `main.py` 的 `APP_HTML_VERSION` |

## 具體的錯誤模式（過去犯過）

```
❌ 錯誤：「CHILD_CAP：兒童 delta/theta 的截斷值大幅提高（成人 delta 98K → 兒童 400K）」
   → 成人 delta 早已改為 500K，98K 是舊的 BrainDNA 原始設計值

✅ 正確：先 Read braindna_algorithms.py 第 29-40 行，確認 CAP["r_delta"] = 500_000 後再陳述
```

## 說「已完成」的唯一標準

- 必須能引用**檔案名稱 + 行號**作為佐證
- 不得僅憑摘要或對話歷史宣稱某事「已做好」

## 當不確定時

直接說：「讓我先確認目前的程式碼狀態」然後用工具讀取，不要猜測。
